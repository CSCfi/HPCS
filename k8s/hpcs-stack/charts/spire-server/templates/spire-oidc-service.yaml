# Service definition for spire-oidc (expose the OIDC socket)
apiVersion: v1
kind: Service
metadata:
  name: spire-oidc
spec:
  type: LoadBalancer
  selector:
    app: spire-server
  ports:
    - name: https
      port: {{ .Values.oidc.port }}
      targetPort: hpcs-nginx
